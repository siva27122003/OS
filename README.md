# OS

```markdown
---

## 📂 Listing Files and Directories

| Command | Description |
|--------|-------------|
| `ls -F` | Lists folders with `/` at the end |
| `ls -a` | Shows hidden files (e.g., `.bashrc`) |
| `ls -R` | Recursively lists all files and subfolders |
| `ls -l` | Long listing with permissions, owner, size, date |

---

